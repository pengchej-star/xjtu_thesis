% experiment.tex

\chapter{实验设计与结果分析}
\echapter{Experiments and Analysis}

\section{实验环境与参数设置}
\esection{Experimental Setup}

本文实验面向长序列多步预测任务，所有实验均基于 TimeMixer 模型及其改进变体开展。输入序列长度统一设置为 96，预测长度分别设置为 96、192、336 和 720。模型训练采用 MSE 损失函数，评价指标为 MSE 和 MAE。为保证消融实验的可比性，在同一数据集和同一预测长度下，除对应改进模块外，其余主要训练设置保持一致。

本文构造 M0、M1、M2、M3、M12、M13、M23 和 M4 共 8 个模型变体，在 8 个数据集和 4 种预测长度上进行实验，共形成 $8 \times 8 \times 4 = 256$ 个模型设置。M0 为 TimeMixer 基线，M1、M2、M3 分别对应单模块改进，M12、M13、M23 和 M4 用于分析模块组合效果。每个设置统计 MSE 和 MAE 两项指标，最终形成 64 个“数据集/预测长度/指标”评价场景。

实验使用固定随机种子 2021，任务类型为长序列预测，特征模式为多变量预测，实验重复次数为 1。优化器采用 Adam，训练损失为 MSE。各数据集的主要参数如表 \ref{tab:hyperparameters} 所示；更具体的软硬件环境信息见附录。

\begin{table}[htbp]
    \centering
    \caption{主要实验参数设置}
    \label{tab:hyperparameters}
    \resizebox{\textwidth}{!}{%
    \begin{tabular}{cccccccc}
        \toprule
        数据集 & 输入长度 & 预测长度 & 学习率 & Batch Size & Epoch & Patience & 主要模型维度 \\
        \midrule
        ETTh1 & 96 & 96/192/336/720 & 0.01 & 128 & 10 & 3 & $d_{model}=16,d_{ff}=32$ \\
        ETTh2 & 96 & 96/192/336/720 & 0.01 & 16 & 10 & 3 & $d_{model}=16,d_{ff}=32$ \\
        ETTm1 & 96 & 96/192/336/720 & 0.01 & 16 & 10 & 3 & $d_{model}=16,d_{ff}=32$ \\
        ETTm2 & 96 & 96/192/336/720 & 0.01 & 128 & 10 & 3 & $d_{model}=32,d_{ff}=32$ \\
        Weather & 96 & 96/192/336/720 & 0.01 & 128 & 10 & 3 & $d_{model}=16,d_{ff}=32$ \\
        ECL & 96 & 96/192/336/720 & 0.01 & 32 & 8 & 2 & $d_{model}=16,d_{ff}=32$ \\
        Traffic & 96 & 96/192/336/720 & 0.01 & 8 & 8 & 2 & $d_{model}=32,d_{ff}=64$ \\
        Solar & 96 & 96/192/336/720 & 0.001 & 32 & 8 & 2 & $d_{model}=512,d_{ff}=2048$ \\
        \bottomrule
    \end{tabular}}
\end{table}

除表中列出的主要参数外，实验默认使用 3 层下采样结构和下采样窗口 2；Solar 数据集下采样层数为 2。M0 使用平均池化下采样和移动平均分解，作为 TimeMixer 基线；M1、M2、M3 分别只加入一个改进模块；M12、M13、M23 和 M4 用于观察模块组合效果。

\section{数据集介绍}
\esection{Datasets}

本文使用 8 个公开时序预测数据集，覆盖电力、气象、交通和能源等场景。数据集基本情况如表 \ref{tab:datasets} 所示。表中的变量数和采样频率来自公开数据集说明和相关文献，其中 Weather、ECL、Traffic 和 Solar 等数据集分别可参考公开气象站数据、UCI 电力负荷数据、PeMS 交通数据和 LSTNet 工作中整理的太阳能数据说明\cite{maxplanck2020weather,dua2019uci,chen2001pems,lai2018lstnet}。

\begin{table}[htbp]
    \centering
    \caption{实验数据集统计表}
    \label{tab:datasets}
    \begin{tabular}{cccl}
        \toprule
        数据集 & 变量数 & 采样频率 & 数据特点 \\
        \midrule
        ETTh1 / ETTh2 & 7 & 1 hour & 电力变压器油温与负载，时间粒度较粗 \\
        ETTm1 / ETTm2 & 7 & 15 min & 电力变压器数据，包含较细粒度变化 \\
        Weather & 21 & 10 min & 多气象指标，变量之间存在一定关联 \\
        ECL & 321 & 1 hour & 多用户用电量，周期性较明显 \\
        Traffic & 862 & 1 hour & 道路占用率，变量数较多且波动复杂 \\
        Solar & 137 & 10 min & 太阳能发电功率，受天气影响较大 \\
        \bottomrule
    \end{tabular}
\end{table}

从数据特点看，ETT 系列变量数较少，适合观察模型在电力变压器数据上的基本表现；Weather、ECL、Traffic 和 Solar 的变量数更多，更能体现多变量建模和模型稳定性的差异。本文不单独追求某一个数据集上的最优结果，而是从多个数据集和预测长度的整体表现分析改进模块在本文设置下的效果差异。

\section{评价指标}
\esection{Evaluation Metrics}

本文采用均方误差（Mean Squared Error, MSE）和平均绝对误差（Mean Absolute Error, MAE）作为评价指标。设真实值为 $y_i$，预测值为 $\hat{y}_i$，样本总数为 $n$，则 MSE 定义为：
\begin{equation}
    \mathrm{MSE} = \frac{1}{n}\sum_{i=1}^{n}(y_i-\hat{y}_i)^2.
\end{equation}
MAE 定义为：
\begin{equation}
    \mathrm{MAE} = \frac{1}{n}\sum_{i=1}^{n}|y_i-\hat{y}_i|.
\end{equation}

MSE 对较大误差更敏感，能够反映模型是否出现明显预测偏差；MAE 直接表示平均绝对误差，更容易从整体误差大小进行理解。两项指标均为数值越低表示预测效果越好。本文在分析实验结果时同时观察 MSE 和 MAE，避免只依据单一指标作出结论。

\section{外部模型对比实验}
\esection{Comparison with External Baselines}

为了观察本文模型在常见时序预测方法中的相对位置，本文引入 TimeMixer 原论文公开的长预测实验结果作为外部参照。外部模型结果的训练环境和部分实验设置不由本文统一控制，因此本节只用于参考性对比，不作为严格统一复现实验。

表 \ref{tab:external_avg} 给出了各数据集在 4 个预测长度上的平均结果，评价指标为 MSE/MAE。表中 M0、M1、M2 和 M4 来自本文固定随机种子 2021、单次实验设置；PatchTST、Autoformer、DLinear、TimesNet、FEDformer 和 Crossformer 的结果来自 TimeMixer 原论文公开结果，完整预测长度明细见附录。

\begin{table}[htbp]
    \centering
    \caption{外部模型与本文主要变体平均结果对比（MSE/MAE）}
    \label{tab:external_avg}
    \resizebox{\textwidth}{!}{%
    \begin{tabular}{c|cccc|cccccc}
        \toprule
        数据集 & M0 & M1 & M2 & M4 & PatchTST & Autoformer & DLinear & TimesNet & FEDformer & Crossformer \\
        \midrule
        ETTh1 & 0.462/0.446 & 0.497/0.473 & 0.451/0.442 & 0.469/0.453 & 0.516/0.484 & 0.496/0.487 & 0.461/0.457 & 0.495/0.450 & 0.498/0.484 & 0.529/0.522 \\
        ETTh2 & 0.387/0.408 & 0.403/0.417 & 0.391/0.412 & 0.415/0.426 & 0.391/0.411 & 0.450/0.459 & 0.563/0.519 & 0.414/0.427 & 0.437/0.449 & 0.942/0.684 \\
        ETTm1 & 0.386/0.399 & 0.394/0.407 & 0.387/0.399 & 0.402/0.411 & 0.406/0.407 & 0.588/0.517 & 0.404/0.408 & 0.400/0.406 & 0.448/0.452 & 0.513/0.495 \\
        ETTm2 & 0.278/0.325 & 0.364/0.374 & 0.278/0.324 & 0.293/0.338 & 0.290/0.334 & 0.327/0.371 & 0.354/0.402 & 0.291/0.333 & 0.305/0.349 & 0.757/0.610 \\
        Weather & 0.245/0.276 & 0.245/0.277 & 0.247/0.276 & 0.242/0.280 & 0.265/0.285 & 0.338/0.382 & 0.265/0.315 & 0.251/0.294 & 0.309/0.360 & 0.264/0.320 \\
        ECL & 0.183/0.273 & 0.187/0.278 & 0.183/0.272 & 0.189/0.286 & 0.216/0.318 & 0.227/0.338 & 0.225/0.319 & 0.193/0.304 & 0.214/0.327 & 0.244/0.334 \\
        Traffic & 0.509/0.314 & 0.543/0.343 & 0.481/0.296 & 0.528/0.355 & 0.529/0.341 & 0.628/0.379 & 0.625/0.383 & 0.620/0.336 & 0.610/0.376 & 0.667/0.426 \\
        Solar & 0.233/0.291 & 0.219/0.271 & 0.250/0.321 & 0.208/0.286 & 0.287/0.333 & 0.586/0.557 & 0.330/0.401 & 0.403/0.374 & 0.328/0.383 & 0.406/0.442 \\
        \bottomrule
    \end{tabular}}
\end{table}

从表 \ref{tab:external_avg} 可以看出，TimeMixer 基线 M0 在 ETTh1、ETTm2、ECL 等数据集上的平均误差接近或低于多类外部模型。本文内部变体中，M2 在 Traffic、ECL、ETTm2 上的平均结果较好；M1 在 Solar 上平均误差较低，但在 ETTm2 和 Traffic 上误差上升；M4 在 Solar 和 Weather 上有一定优势，但在 Traffic 和 ETTh2 上不如 M0 或 M2。外部比较和内部消融结果共同表明，不同模块的收益与数据集特征有关。

\section{主实验结果}
\esection{Main Results}

表 \ref{tab:main_results} 给出了 8 个模型变体在 8 个数据集和 4 种预测长度上的完整结果。由于结果表较宽，表中保留了 MSE/MAE 并列展示的形式，粗体表示同一数据集和同一预测长度下对应指标的最优值。

\input{tables/main_results.tex}

从完整结果可以看出，在本文固定随机种子 2021、实验重复次数为 1 的设置下，M0 在多个数据集和预测长度上仍能取得最优或接近最优的结果。M2 的最优项主要集中在 ETTh1、ETTm1、ETTm2、ECL 和 Traffic 等数据集，其中 Traffic 的多个预测长度均出现 M2 优于其他变体的情况。M1 和 M3 的收益更分散，部分预测长度上误差下降，但也存在误差上升的场景。M4 虽然包含全部三个改进模块，但最优次数为 5 次，少于 M0 和 M2。

\begin{figure}[htbp]
    \centering
    \includegraphics[width=0.95\textwidth]{thesis/fig_ablation_wins_bar_chart.png}
    \caption{各模型变体最优次数柱状图}
    \label{fig:wins_bar_chart}
\end{figure}

\section{单模块消融实验分析}
\esection{Single-module Ablation Analysis}

为观察单个模块的独立作用，可以重点比较 M0、M1、M2 和 M3。M1 只替换下采样模块，M2 只替换分解模块，M3 只加入通道混合模块。

在三个单模块变体中，M2 的最优次数最多。它在 ETTh1、ETTm1、ETTm2、ECL 和 Traffic 等数据集的若干预测长度上取得最优结果；其中 Traffic 数据集的多个预测长度下均出现 M2 的 MSE 或 MAE 更低。Traffic 数据中既有日常出行周期，也有局部突发拥堵，单一窗口分解难以同时覆盖这些变化尺度，多核窗口则提供了更灵活的趋势估计。

M1 的结果波动较大。深度可分离卷积下采样可以让模型学习下采样权重，有机会保留局部突变信息，但它也可能引入额外噪声或改变原始序列的平滑结构。对于 ETTm 等细粒度数据，M1 在部分预测长度上能取得较好结果；但在一些较平滑或预测步长较大的场景中，它未必优于平均池化。

M3 的收益同样较分散。该模块在 Weather 等变量相关性较强的数据上有时能取得较好表现，但没有形成连续优势。一个可能原因是当前通道混合只在多尺度序列进入通道独立建模前进行一次浅层低秩映射，对复杂变量关系的表达能力有限。同时，在通道独立主干结构前加入额外变量交互，也可能改变后续分解和混合过程面对的输入分布，使优化难度增加。

\section{组合消融实验分析}
\esection{Combined Ablation Analysis}

除了单模块实验，本文还补充了 M12、M13 和 M23 三个两两组合变体，并与 M4 完整模型进行比较。这样可以进一步观察模块之间是否存在互补关系或耦合干扰。

表 \ref{tab:wins} 给出了各模型变体在 64 个评价场景中的胜出次数。M2 的总胜出次数为 22 次，是所有变体中最高的；M0 获得 19 次，说明原始模型仍然是较强基线；M4 仅获得 5 次，低于 M2 和 M0，也低于部分组合变体的局部表现。

\begin{table}[htbp]
    \centering
    \caption{各模型变体最优次数统计}
    \label{tab:wins}
    \begin{tabular}{cccc}
        \toprule
        变体 & MSE最优次数 & MAE最优次数 & 总计 \\
        \midrule
        M0 & 10 & 9 & 19 \\
        M1 & 1 & 2 & 3 \\
        M2 & 10 & 12 & 22 \\
        M3 & 2 & 2 & 4 \\
        M12 & 2 & 4 & 6 \\
        M13 & 1 & 2 & 3 \\
        M23 & 1 & 1 & 2 \\
        M4 & 5 & 0 & 5 \\
        \bottomrule
    \end{tabular}
\end{table}

从组合结果看，多个模块同时加入并不一定带来线性叠加收益。进一步比较各组合变体与 M4 的结果可见：M12 在 MSE 上有 14/32 个场景优于 M4，在 MAE 上有 19/32 个场景优于 M4；M13 分别为 11/32 和 15/32；M23 分别为 18/32 和 20/32。也就是说，在部分数据集和预测长度下，去掉某个模块后的组合变体反而取得更低误差。

造成这一现象的原因可能有三点。首先，M1 的可学习下采样和 M2 的多核分解都在处理输入特征与趋势信息，二者功能上可能存在一定重叠。其次，M3 改变了变量维度的信息分布，可能使后续分解模块面对的输入特征与原始设定不同。最后，三个模块同时加入后参数和优化路径都变得更复杂，在固定训练设置下未必能够充分发挥作用。因此，M4 更适合作为结构完整变体参与对照，而不是被直接视为整体最优方案。

\section{实验结果讨论}
\esection{Discussion}

综合实验结果，本文主要得到以下几点认识。

第一，在本文实验设置下，多核门控分解带来的收益最集中。M2 获得 22 次最优，其中 MAE 最优次数为 12 次，高于其他变体。该模块直接作用于 TimeMixer 的分解环节，使模型能够利用多个窗口估计趋势项，并通过样本级门控权重融合不同尺度结果。

第二，M0 获得 19 次最优，说明原始多尺度分解混合结构本身仍是较强基线。本文的改进结果更适合理解为对不同结构环节的对照分析，而不是简单的整体替换。

第三，M1 和 M3 分别获得 3 次和 4 次最优，说明可学习下采样和浅层通道混合在当前设置下只在少数场景中降低误差。后续若继续改进，可以考虑为 M1 增加更明确的平滑约束，或将 M3 从单次输入混合扩展为分层通道交互。

第四，M4 只获得 5 次最优，低于 M0、M2 和 M12。这一结果说明，在轻量级时序预测模型中，多个结构模块同时加入后可能改变输入分布和优化路径，模块组合需要通过消融实验具体判断。

\begin{figure}[htbp]
    \centering
    \includegraphics[width=0.92\textwidth]{thesis/fig_traffic_m0_m2_forecast_case.png}
    \caption{Traffic数据集上M0与M2的代表性预测结果对比}
    \label{fig:traffic_prediction_case}
\end{figure}

\section{应用场景分析}
\esection{Application Scenario Analysis}

本文使用的数据集覆盖电力、气象、交通和能源等场景。与泛化到具体业务系统相比，本文更关注不同数据特征下的结构选择问题。

在电力相关数据中，ETT 系列变量数较少，ECL 变量数较多且周期性较明显。实验结果显示，M0 在部分 ETT 场景中仍能取得最优或接近最优的结果，而 M2 在 ECL 上的平均误差与 M0 接近并多次取得较低指标。这说明对于周期性较强的数据，改进分解窗口比单纯增加模块数量更值得优先考虑。

在交通场景中，Traffic 数据集变量数达到 862，既包含出行周期，也包含局部拥堵造成的波动。本文实验中 M2 在 Traffic 上的平均 MSE/MAE 为 0.481/0.296，低于 M0 的 0.509/0.314 和 M4 的 0.528/0.355，说明多核门控分解对复杂周期和局部波动并存的数据更有针对性。相反，M3 未在 Traffic 上形成优势，说明浅层变量交互不足以替代更强的空间或变量关系建模。

在气象和能源场景中，Weather 具有多气象变量相关性，Solar 受昼夜周期和天气变化共同影响。M4 在 Solar 上的平均 MSE/MAE 为 0.208/0.286，是表 \ref{tab:external_avg} 中本文内部变体的较低结果；但在 Weather 上，M4 的 MAE 并未低于 M0 和 M2。这说明完整模型在部分能源数据上可能受益于模块组合，但不能据此推出更复杂结构在所有场景中更合适。
